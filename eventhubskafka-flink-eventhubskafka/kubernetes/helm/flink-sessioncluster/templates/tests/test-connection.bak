apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flink-sessioncluster.fullname" . }}-test-connection"
  labels:
{{ include "flink-sessioncluster.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flink-sessioncluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
